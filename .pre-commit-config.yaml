repos:
  - repo: local
    hooks:
      - id: tests
        name: tests
        entry: uv run pytest --cov=slack_codex_bridge --cov-report=term-missing --cov-fail-under=100
        language: system
        pass_filenames: false
        always_run: true
